---
phase: 01-foundation-compatibility-gate-ci-hardening-ts-js-depth
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - Cargo.toml
  - src/grammar.rs
  - docs/supported-languages.md
  - .planning/phases/01-foundation-compatibility-gate-ci-hardening-ts-js-depth/01-COMPAT-VERDICTS.md
autonomous: true
requirements: [COMPAT-01, COMPAT-02]

must_haves:
  truths:
    - "Elixir, Erlang, Gleam, and Haskell — the STACK.md-vs-FEATURES.md disputed 4 — each have an empirically-run verdict (never inferred from declared semver), resolving D-03's dispute"
    - "All 15 total candidates from COMPAT-01 (the 11 from 01-01 plus these 4) have one recorded row in 01-COMPAT-VERDICTS.md: crate, version, tree-sitter-language requirement, ABI test result, license"
    - "Any candidate (among the 4 disputed) that fails the gate has an honest docs/supported-languages.md note per CONTRIBUTING §'When a Language Has No Usable Grammar' — crate checked, version, exact requirement found"
    - "F# is moved out of its current 🔴-blocked docs row (it passed in 01-01) and Vue/Apex/Liquid/COBOL each carry the precise blocked-reason note from D-06, not the current vaguer placeholder text"
  artifacts:
    - path: Cargo.toml
      provides: "up to 4 additional grammar-only optional deps + feature flags for whichever of elixir/erlang/gleam/haskell pass (failing ones are reverted, per D-01)"
    - path: src/grammar.rs
      provides: "grammar accessor fn + ABI-test arm for each passing disputed candidate"
    - path: .planning/phases/01-foundation-compatibility-gate-ci-hardening-ts-js-depth/01-COMPAT-VERDICTS.md
      provides: "one row per ALL 15 COMPAT-01 candidates: crate, version, tree-sitter-language req, ABI result, license (D-04)"
    - path: docs/supported-languages.md
      provides: "F# row flipped 🔴→🟠; Vue/Apex/Liquid/COBOL rows carry precise blocked-reason notes; any disputed candidate that failed gets an honest failure note (D-05/D-06)"
  key_links:
    - from: "Cargo.toml `elixir = [\"dep:tree-sitter-elixir\"]` (if it passes)"
      to: "src/grammar.rs `#[cfg(feature = \"elixir\")] pub fn elixir()`"
      via: "matching Cargo feature name gates the grammar.rs fn"
      pattern: "cfg\\(feature = \"elixir\"\\)"
    - from: "01-COMPAT-VERDICTS.md row for a FAILED disputed candidate"
      to: "docs/supported-languages.md's matching row Notes column"
      via: "the honest-failure note appears in both places, per D-04+D-05"
      pattern: "no usable grammar|not compatible|blocked"
---

<objective>
Resolve the one genuine open empirical question COMPAT-01 leaves after 01-01: whether Elixir, Erlang,
Gleam, and Haskell — which STACK.md calls compatible (via their normal `tree-sitter-language ^0.1`
dependency) but FEATURES.md calls incompatible (looking only at their `tree-sitter ^0.23`
dev-dependency) — actually pass this repo's real ABI gate. Per D-03, resolve this by running the test,
not by re-reading either doc. Then close out COMPAT-01/COMPAT-02 for all 15 candidates: write the full
`01-COMPAT-VERDICTS.md` artifact (D-04) and fold the honest docs corrections research already
identified (D-05/D-06) — F#'s unblock, and precise blocked-reason notes for Vue/Apex/Liquid/COBOL.

Purpose: this is the second and final half of the compatibility gate. Phase 4's scope for the
BEAM/Haskell family (LANG-12) is directly conditional on this plan's verdicts — a wrong or skipped
verdict here silently mis-scopes a later phase.

Output: up to 4 more grammar-only Cargo features (any that fail are reverted with their failure
recorded, not left half-wired), a complete 15-row `01-COMPAT-VERDICTS.md`, and
`docs/supported-languages.md` corrected per D-05/D-06.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-compatibility-gate-ci-hardening-ts-js-depth/01-CONTEXT.md
@.planning/phases/01-foundation-compatibility-gate-ci-hardening-ts-js-depth/01-RESEARCH.md
@.planning/phases/01-foundation-compatibility-gate-ci-hardening-ts-js-depth/01-01-PLAN.md
@CONTRIBUTING.md
@Cargo.toml
@src/grammar.rs
@docs/supported-languages.md

<interfaces>
<!-- Verified exported LanguageFn constant per disputed candidate (docs.rs, 2026-07-05), same
     verification method 01-01 used for the 11 expected-compatible candidates. -->
tree-sitter-elixir 0.3.5  -> exports plain `LANGUAGE`
tree-sitter-erlang 0.19.0 -> exports plain `LANGUAGE`
tree-sitter-gleam 1.0.0   -> exports plain `LANGUAGE`
tree-sitter-haskell 0.23.1 -> exports plain `LANGUAGE`
(none of the 4 disputed candidates has the OCaml/F#-style non-plain-LANGUAGE naming exception.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire and empirically gate the 4 disputed candidates — Elixir, Erlang, Gleam, Haskell</name>
  <files>Cargo.toml, src/grammar.rs</files>
  <read_first>
    - src/grammar.rs (post-01-01 state: fns/ABI-test arms now end after the 01-01 batch — `fsharp()` and its `check(...)` arm are the last entries before `#[cfg(test)] mod tests`'s closing brace)
    - Cargo.toml (post-01-01 state: `[dependencies]`/`[features]` blocks now end after the `fsharp` entries)
    - .planning/phases/01-foundation-compatibility-gate-ci-hardening-ts-js-depth/01-RESEARCH.md, "On the STACK.md vs. FEATURES.md disagreement" section (why these 4 are disputed, and why the dev-dependency `tree-sitter ^0.23` claim is not the real gate)
  </read_first>
  <action>
    Same 3-edit recipe 01-01 used for its 11 candidates, applied to the 4 disputed candidates — this
    is the empirical tie-breaker D-03 calls for; do not skip wiring any of the 4 on the assumption that
    FEATURES.md's dev-dependency reading already answered the question.

    **Cargo.toml** — append to `[dependencies]` (after the 01-01 batch's `tree-sitter-fsharp` line):
    ```toml
    tree-sitter-elixir = { version = "0.3.5", optional = true }
    tree-sitter-erlang = { version = "0.19.0", optional = true }
    tree-sitter-gleam = { version = "1.0.0", optional = true }
    tree-sitter-haskell = { version = "0.23.1", optional = true }
    ```
    and to `[features]` (grammar-only, no `_extractors`, per D-02):
    ```toml
    elixir = ["dep:tree-sitter-elixir"]
    erlang = ["dep:tree-sitter-erlang"]
    gleam = ["dep:tree-sitter-gleam"]
    haskell = ["dep:tree-sitter-haskell"]
    ```

    **src/grammar.rs** — insert after `fsharp()`, before `#[cfg(test)] mod tests`:
    ```rust
    #[cfg(feature = "elixir")]
    /// Returns the tree-sitter grammar for Elixir.
    pub fn elixir() -> Language {
        tree_sitter_elixir::LANGUAGE.into()
    }

    #[cfg(feature = "erlang")]
    /// Returns the tree-sitter grammar for Erlang.
    pub fn erlang() -> Language {
        tree_sitter_erlang::LANGUAGE.into()
    }

    #[cfg(feature = "gleam")]
    /// Returns the tree-sitter grammar for Gleam.
    pub fn gleam() -> Language {
        tree_sitter_gleam::LANGUAGE.into()
    }

    #[cfg(feature = "haskell")]
    /// Returns the tree-sitter grammar for Haskell.
    pub fn haskell() -> Language {
        tree_sitter_haskell::LANGUAGE.into()
    }
    ```

    **src/grammar.rs test mod** — append after the `fsharp` check arm:
    ```rust
    #[cfg(feature = "elixir")]
    check("elixir", super::elixir());
    #[cfg(feature = "erlang")]
    check("erlang", super::erlang());
    #[cfg(feature = "gleam")]
    check("gleam", super::gleam());
    #[cfg(feature = "haskell")]
    check("haskell", super::haskell());
    ```

    **Verify per candidate — run BOTH commands** (the additive ABI test alone can mask an isolation
    break, per 01-RESEARCH.md Pitfall 4):
    ```bash
    cargo test grammar::tests::abi_versions_are_compatible --features <lang>
    cargo check --no-default-features --features <lang>
    ```

    **Contingency (if any of the 4 fails either command):** revert that candidate's 3 edits (keep any
    others that passed), and record the failure verbatim (exact error text, crate name + version
    checked, the actual `tree-sitter`/`tree-sitter-language` requirement found) — Task 2 writes this
    into both `01-COMPAT-VERDICTS.md` and `docs/supported-languages.md`, per D-04/D-05. A candidate
    that fails must not remain half-wired in `Cargo.toml`/`src/grammar.rs`.

    After all 4 are resolved (whether wired-and-passing or reverted-with-recorded-failure), run the
    full regression:
    ```bash
    cargo test --workspace           # default features unaffected
    cargo build --all-features       # every currently-wired feature (22 + 11 + however many of these 4 passed) coexists
    ```
  </action>
  <acceptance_criteria>
    - For each of `elixir erlang gleam haskell`: EITHER (a) both `cargo test grammar::tests::abi_versions_are_compatible --features <lang>` and `cargo check --no-default-features --features <lang>` exit 0, OR (b) the candidate's 3 edits are fully reverted (`grep -c '<lang> = \[' Cargo.toml` returns 0) and its failure is captured for Task 2
    - `grep -n '^default = \[' Cargo.toml` does NOT contain `"elixir"`, `"erlang"`, `"gleam"`, or `"haskell"` regardless of pass/fail (D-02: never added to default)
    - `cargo test --workspace` exits 0 (default-features regression)
    - `cargo build --all-features` exits 0 (full coexistence of every currently-wired feature)
  </acceptance_criteria>
  <verify>
    <automated>for l in elixir erlang gleam haskell; do cargo test grammar::tests::abi_versions_are_compatible --features $l && cargo check --no-default-features --features $l || echo "FAIL: $l (revert this candidate's edits before Task 2)"; done && cargo test --workspace && cargo build --all-features</automated>
  </verify>
  <done>All 4 disputed candidates have a real, empirically-run verdict; passing ones are wired in the tree exactly like 01-01's batch, failing ones are cleanly reverted with their exact failure captured for the verdicts artifact.</done>
</task>

<task type="auto">
  <name>Task 2: Write 01-COMPAT-VERDICTS.md (all 15) and correct docs/supported-languages.md</name>
  <files>.planning/phases/01-foundation-compatibility-gate-ci-hardening-ts-js-depth/01-COMPAT-VERDICTS.md, docs/supported-languages.md</files>
  <read_first>
    - docs/supported-languages.md (current rows for Elixir, Erlang, Gleam, Zig, Julia, R, Haskell, OCaml, Objective-C, Fortran, Groovy, PowerShell, SystemVerilog, Astro, Vue, Liquid, F#, Salesforce Apex, COBOL — and the "When a Language Has No Usable Grammar" honest-failure protocol these must follow)
    - CONTRIBUTING.md §"When a Language Has No Usable Grammar" (the exact honest-failure note format: crate checked, version, exact tree-sitter/tree-sitter-language requirement found)
    - Task 1's actual results (pass/fail per elixir/erlang/gleam/haskell) and 01-01-PLAN.md's Tasks 2-3 results (pass/fail per the 11 expected-compatible candidates — expected all-pass per STACK.md, but verify against what actually happened, not the expectation)
  </read_first>
  <action>
    **1. Write `.planning/phases/01-foundation-compatibility-gate-ci-hardening-ts-js-depth/01-COMPAT-VERDICTS.md`**
    per D-04 — one row per ALL 15 COMPAT-01 candidates (the 11 from 01-01 + the 4 from Task 1), columns:
    Language | Crate | Version pinned | `tree-sitter-language` requirement found | ABI test result
    (PASS/FAIL) | License. Use the crate/version/license data already verified in 01-RESEARCH.md's
    Standard Stack table (all 15 declare `tree-sitter-language = "^0.1"` as a normal dependency — the
    real gate) cross-checked against Task 1's and 01-01's actual run results — if any candidate's
    real-world result differs from the expected PASS, record what actually happened, not the
    expectation. Example row:
    ```markdown
    | Language | Crate | Version | tree-sitter-language req | ABI Result | License |
    |----------|-------|---------|---------------------------|------------|---------|
    | Zig | tree-sitter-zig | 1.1.2 | ^0.1 | PASS | MIT |
    | Elixir | tree-sitter-elixir | 0.3.5 | ^0.1 | PASS or FAIL (per Task 1) | Apache-2.0 |
    ```
    Include a short header noting the empirical method used (never inferred from declared semver,
    per D-01/D-03) and the date.

    **2. Update `docs/supported-languages.md`** — three distinct edits:

    a. **F# unblock (D-06):** change the F# row's Status cell from `🔴` to `🟠` (it passed in 01-01),
       and update its Notes cell from "grammar availability/compat to verify" to something like
       "tree-sitter-fsharp (ionide) — ABI-verified compatible; extractor lands in Phase 4 (LANG-11)".

    b. **Disputed-4 rows (Elixir/Erlang/Gleam/Haskell):** if a candidate PASSED Task 1, its row stays
       `🟠` (D-05: "Passing candidates keep their 🟠 row until their extractor phase flips them to
       🟢") — optionally tighten its Notes cell to reflect the confirmed ABI-pass. If a candidate
       FAILED, flip its Status cell to `🔴` and replace its Notes cell with the honest failure note
       per CONTRIBUTING's protocol: exact crate name, version checked, and the precise incompatibility
       found (e.g. "tree-sitter-X 0.y.z requires tree-sitter-language ^0.2, incompatible with this
       repo's `>=0.24, <0.27` window" — use whatever Task 1's actual error showed, do not guess).

    c. **Vue/Apex/Liquid/COBOL precise blocked-reason notes (D-06):** replace each row's vague Notes
       cell with the specific, already-researched reason:
       - Vue: "tree-sitter-vue pins `tree-sitter ~0.20` as a normal dependency — old incompatible
         `Language` type, unmaintained since 2022"
       - Salesforce Apex: same reason as Vue (tree-sitter ~0.20 as a normal dependency, unmaintained)
       - Liquid: "no crate exists on crates.io under `tree-sitter-liquid` or any known variant
         (verified 2026-07-05)"
       - COBOL: "only crate `tree-sitter-cobol 0.1.0` declares zero dependencies and no repository —
         not a functioning grammar integration, not merely an old version"

    Do not touch any other row (the 11 expected-compatible candidates from 01-01 keep their existing
    🟠 rows/notes unless you choose to also tighten their Notes with the confirmed-pass detail — that
    part is optional polish, not required).
  </action>
  <acceptance_criteria>
    - `.planning/phases/01-foundation-compatibility-gate-ci-hardening-ts-js-depth/01-COMPAT-VERDICTS.md` exists with exactly 15 language data rows (plus header)
    - `grep -c 'PASS\|FAIL' .planning/phases/01-foundation-compatibility-gate-ci-hardening-ts-js-depth/01-COMPAT-VERDICTS.md` equals 15
    - `grep -n '| F#' docs/supported-languages.md` shows `🟠` (not `🔴`) in the Status column
    - `grep -n '| Vue' -A0 docs/supported-languages.md` and the `Salesforce Apex` row both mention "tree-sitter ~0.20" or "unmaintained"
    - `grep -n '| Liquid' docs/supported-languages.md` mentions "no crate exists"
    - `grep -n '| COBOL' docs/supported-languages.md` mentions "zero dependencies" or "no repository"
    - `cargo test --workspace` still exits 0 (docs-only + verdicts-artifact changes must not touch any test-affecting code)
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/01-foundation-compatibility-gate-ci-hardening-ts-js-depth/01-COMPAT-VERDICTS.md && grep -c 'PASS\|FAIL' .planning/phases/01-foundation-compatibility-gate-ci-hardening-ts-js-depth/01-COMPAT-VERDICTS.md && cargo test --workspace</automated>
  </verify>
  <done>01-COMPAT-VERDICTS.md records all 15 COMPAT-01 candidates' real ABI results; docs/supported-languages.md reflects F#'s unblock and precise Vue/Apex/Liquid/COBOL blocked-reasons, plus an honest failure note for any disputed candidate that failed Task 1.</done>
</task>

</tasks>

<verification>
Run, in order: `cargo test --workspace` (default features unaffected), then for each of the 4 disputed
candidates that ended up wired (`elixir erlang gleam haskell` — whichever passed) both
`cargo test grammar::tests::abi_versions_are_compatible --features <lang>` and
`cargo check --no-default-features --features <lang>`, then `cargo build --all-features`. Finally,
confirm `01-COMPAT-VERDICTS.md` has exactly 15 rows and `docs/supported-languages.md`'s F#/Vue/Apex/
Liquid/COBOL rows carry the corrected text.
</verification>

<success_criteria>
- The Elixir/Erlang/Gleam/Haskell STACK-vs-FEATURES dispute is resolved empirically (D-03), not by
  re-reading either research doc.
- `01-COMPAT-VERDICTS.md` has one row per all 15 COMPAT-01 candidates (D-04).
- `docs/supported-languages.md` carries F#'s unblock and the Vue/Apex/Liquid/COBOL precise
  blocked-reason notes (D-06), plus an honest failure note for any disputed candidate that failed.
- No regression in the existing default or all-features build.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-compatibility-gate-ci-hardening-ts-js-depth/01-02-SUMMARY.md`
</output>
