---
phase: 04-risky-novel-design-extractors
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - src/lang.rs
  - Cargo.toml
  - bindings/node/Cargo.toml
  - bindings/python/Cargo.toml
  - src/extract/gleam.rs
  - src/extract/mod.rs
  - src/extract/dispatch.rs
  - eval/corpus/gleam/scoped_call/math.gleam
  - eval/corpus/gleam/scoped_call/expected.edges
  - docs/supported-languages.md
autonomous: true
requirements: [LANG-12]

must_haves:
  truths:
    - "extract() on a .gleam file emits function/type symbols with REAL Public/Private visibility from visibility_modifier child presence"
    - "extract() detects both free calls and module-qualified calls (field_access shape)"
    - "import statements are emitted as Import references"
    - "cargo test --no-default-features --features gleam passes standalone"
  artifacts:
    - path: "src/extract/gleam.rs"
      provides: "GleamExtractor — pub/fn visibility via child presence, algebraic type declarations, free + qualified call detection, import handling"
      min_lines: 120
    - path: "eval/corpus/gleam/scoped_call/expected.edges"
      provides: "golden same-file Call edge"
    - path: "docs/supported-languages.md"
      provides: "Gleam row moved 🟠 → 🟢 with honest capability columns"
  key_links:
    - from: "src/extract/dispatch.rs"
      to: "src/extract/gleam.rs"
      via: "GleamExtractor match arm under #[cfg(feature = \"gleam\")]"
      pattern: "GleamExtractor"
    - from: "bindings/node/Cargo.toml"
      to: "Cargo.toml"
      via: "explicit features list gains \"gleam\" in the same change"
      pattern: "\"gleam\""
---

<objective>
Ship the Gleam extractor end-to-end — the cleanest member of the BEAM/Haskell family (D-07: full
table stakes expected, no discretionary ceilings needed). `Language::Gleam` enum variant, `pub fn`/`fn`
symbols with REAL visibility, `type` declarations, `import` → Imports, module-qualified calls.

Purpose: First of the four LANG-12 BEAM/Haskell-family languages (Gleam → Elixir → Erlang → Haskell
per D-09), establishing that this family's real-visibility signal (unlike Julia/R/OCaml/F#'s
honest-ceiling approach) is extracted, not guessed.
Output: `src/extract/gleam.rs`, updated `src/lang.rs`/`Cargo.toml`/both bindings `Cargo.toml`s,
`eval/corpus/gleam/scoped_call/`, updated docs row.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-risky-novel-design-extractors/04-CONTEXT.md
@.planning/phases/04-risky-novel-design-extractors/04-RESEARCH.md
@src/extract/objc.rs
@src/extract/fortran.rs
@src/extract/support.rs
@src/lang.rs
@src/extract/mod.rs
@src/extract/dispatch.rs
</context>

<interfaces>
`Descriptor` variants used here: `Namespace(String)`, `Type(String)`, `Term(String)`,
`Method { name: String, disambiguator: String }` (disambiguator = `String::new()` — Gleam has no
arity-based overload identity concern).
`support.rs` helpers as listed in 04-01's `<interfaces>` block (same set; re-read
`src/extract/support.rs` directly rather than duplicating the full signature list here).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Wire Language::Gleam + Cargo feature flip + bindings feature lists</name>
  <read_first>src/extract/objc.rs, src/extract/fortran.rs, src/extract/support.rs, src/lang.rs (whole file), Cargo.toml (features block), bindings/node/Cargo.toml, bindings/python/Cargo.toml</read_first>
  <files>src/lang.rs, Cargo.toml, bindings/node/Cargo.toml, bindings/python/Cargo.toml</files>
  <action>
1. `src/lang.rs`: add `Gleam, // .gleam` to the `Language` enum (append after `FSharp`). Add
   `Language::Gleam` to `Language::ALL`. Add `Language::Gleam => &["gleam"],` to `extensions()`.
   Add `Language::Gleam => "gleam",` to `as_str()`. Add `Language::Gleam` to `assert_variant_in_all`.

2. `Cargo.toml`: change `gleam = ["dep:tree-sitter-gleam"]` to
   `gleam = ["dep:tree-sitter-gleam", "_extractors"]`. Append `"gleam"` to `default = [...]` right
   after `"fsharp"`.

3. `bindings/node/Cargo.toml` AND `bindings/python/Cargo.toml`: append `"gleam"` to the `features`
   list on the `code2graph_core` dependency line in BOTH files (both currently end
   `..., "ocaml", "fsharp"]`).

Do NOT create `src/extract/gleam.rs` yet — Task 2.
  </action>
  <verify>
    <automated>cargo check --no-default-features --features gleam</automated>
    <automated>cargo test --lib lang::tests</automated>
  </verify>
  <acceptance_criteria>
    <check>grep -q "Gleam," src/lang.rs</check>
    <check>grep -q '"gleam"' src/lang.rs</check>
    <check>grep -q 'gleam = \["dep:tree-sitter-gleam", "_extractors"\]' Cargo.toml</check>
    <check>grep -q '"fsharp", "gleam"' Cargo.toml</check>
    <check>grep -q '"fsharp", "gleam"' bindings/node/Cargo.toml</check>
    <check>grep -q '"fsharp", "gleam"' bindings/python/Cargo.toml</check>
  </acceptance_criteria>
  <done>Language::Gleam variant compiles with .gleam extension dispatch; gleam feature gains
  _extractors and sits in default; both bindings' feature lists updated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GleamExtractor — real visibility, algebraic types, free/qualified calls</name>
  <read_first>src/extract/objc.rs, src/extract/fortran.rs, src/extract/support.rs, src/extract/go.rs (capital-export visibility precedent, cited for family style)</read_first>
  <files>src/extract/gleam.rs, src/extract/mod.rs, src/extract/dispatch.rs</files>
  <behavior>
    - Test: `pub fn public_area(shape: Shape) -> Float { ... }` → a Function symbol "public_area" with
      `visibility == Visibility::Public` (a `(visibility_modifier)` child IS present, positionally, as
      the function node's first child).
    - Test: `fn private_helper(x: Int) -> Int { x + 1 }` → a Function symbol with
      `visibility == Visibility::Private` (NO `visibility_modifier` child).
    - Test: `pub type Shape { Circle(radius: Float) Square(side: Float) }` → an Enum symbol "Shape"
      with `visibility == Visibility::Public`.
    - Test: `import gleam/list` → an Import reference "list" (or the module path as written — pick one
      convention and assert it consistently; document the choice in the module doc).
    - Test: `import gleam/io.{println} as io_alias` → an Import reference for the module AND does not
      crash on the `{println}` unqualified-import list or the `as` alias (table stakes: capture the
      base module import; the unqualified names/alias are Claude's-discretion — document what is/is
      not modeled).
    - Test: `private_helper(1)` (free call) → a Call reference "private_helper".
    - Test: `list.length([1,2,3])` (module-qualified call via `field_access`) → a Call reference
      "length" with `qualifier == Some("list")`.
  </behavior>
  <action>
Create `src/extract/gleam.rs`. Module doc header: state D-07's scope (real pub/fn visibility, type
declarations, import, module-qualified calls — "cleanest of the family, full table stakes expected").
No CEILING beyond: algebraic `type` declarations map uniformly to `SymbolKind::Enum` (no
record/variant sub-distinction in v1); Gleam has no cross-file NAMESPACE-style concept.

**Namespace derivation**: `gleam_namespaces(file)` — strip `.gleam`, strip `src/`, split on `/`.

**Symbol collection** (verified AST, 04-RESEARCH.md "Gleam" Code Examples section):
- `import module: (module) ...` → walk recursively, emit `RefRole::Import` (name = the `module` node's
  text, e.g. `gleam/list`; if an `alias:` field is present use its text as an additional/alternate
  detail only if trivially available — do not over-engineer, table stakes is the one Import ref per
  `import` statement).
- `type_definition (visibility_modifier)? (type_name name: (type_identifier)) (data_constructors ...)`
  → `SymbolKind::Enum`. Visibility = `Public` if a `(visibility_modifier)` child is present (positional,
  any node with `pub`), else `Private` — per D-07/research: Gleam is fully module-hidden like Rust, no
  Internal concept.
- `function name: (identifier) parameters: (...) return_type: (...) body: (function_body ...)` →
  `SymbolKind::Function`. Visibility: same `visibility_modifier`-presence check as the type case.

Use the standard `seen: HashSet<String>` + `push_symbol` dedup wrapper (matches every prior
extractor's convention even though no collisions are expected here).

**Calls**:
```rust
const CALL_QUERY: &str = r#"
[
  (function_call function: (identifier) @callee)
  (function_call function: (field_access record: (identifier) @qualifier field: (label) @callee))
]
"#;
```
Run via `collect_call_references`.

**Wiring**: `src/extract/mod.rs` — `#[cfg(feature = "gleam")] pub mod gleam;` /
`#[cfg(feature = "gleam")] pub use gleam::GleamExtractor;` (alphabetical position, near `go`/`groovy`).
`src/extract/dispatch.rs` — matching `use` + `Language::Gleam => GleamExtractor.extract(source, file),`
arm.

Minimal `scopes[0]` = `Module` file-root scope, `attach_reference_scopes`,
`definition_bindings(&defs)`.
  </action>
  <verify>
    <automated>cargo test --no-default-features --features gleam extract::gleam::tests</automated>
    <automated>cargo test --no-default-features --features gleam</automated>
  </verify>
  <acceptance_criteria>
    <check>grep -q "struct GleamExtractor" src/extract/gleam.rs</check>
    <check>grep -q "CALL_QUERY" src/extract/gleam.rs</check>
    <check>grep -q "visibility_modifier" src/extract/gleam.rs</check>
    <check>grep -q "GleamExtractor" src/extract/dispatch.rs</check>
    <check>grep -q "pub mod gleam" src/extract/mod.rs</check>
  </acceptance_criteria>
  <done>GleamExtractor compiles, is wired into dispatch, and its unit tests prove: REAL Public/Private
  visibility from visibility_modifier presence for both functions and types; free and
  module-qualified calls; import handling.</done>
</task>

<task type="auto">
  <name>Task 3: Corpus case + docs row + bindings verify</name>
  <read_first>src/extract/gleam.rs (from Task 2), eval/corpus/fortran/scoped_call/ (format example), docs/supported-languages.md, src/extract/support.rs</read_first>
  <files>eval/corpus/gleam/scoped_call/math.gleam, eval/corpus/gleam/scoped_call/expected.edges, docs/supported-languages.md</files>
  <action>
**Corpus case** (`eval/corpus/gleam/scoped_call/`):

`math.gleam`:
```gleam
fn get_base() -> Int {
  41
}

pub fn compute() -> Int {
  get_base() + 1
}
```

`expected.edges`:
```
# Same-file call: `compute` calls `get_base`, both defined in math.gleam.
# Proves Gleam free-call (function_call) detection resolves via Tier-A
# same-file matching.
math.gleam:5 Call math.gleam:1
```
(Verify exact line numbers against the real parse before committing.)

**Docs row** (`docs/supported-languages.md`): replace the Gleam 🟠 row (`.gleam`) with:
```
| Gleam           | `.gleam`                                |   🟢   |   ✓   |    ✓    |    —    |    —     |            |           | cleanest of the phase family — REAL `pub`/`fn` visibility via `visibility_modifier` child presence; algebraic `type` declarations mapped to SymbolKind::Enum (no record/variant sub-distinction); no dispatch/generics modeling |
```

**Bindings verify (BIND-02)**: `cd bindings/node && npx napi build --release --platform`, confirm
`index.js`/`index.d.ts` diff is a no-op.
  </action>
  <verify>
    <automated>cargo test --no-default-features --features gleam</automated>
    <automated>cargo test --all-features corpus</automated>
    <automated>cd bindings/node && npx napi build --release --platform && git diff --exit-code index.js index.d.ts</automated>
    <automated>cargo test --lib lang::tests::supported_languages_doc_lists_each_primary_extension</automated>
  </verify>
  <acceptance_criteria>
    <check>test -f eval/corpus/gleam/scoped_call/expected.edges</check>
    <check>grep -q "Gleam" docs/supported-languages.md</check>
  </acceptance_criteria>
  <done>Corpus case resolves its expected Call edge; docs row is 🟢 with honest capability columns;
  napi build produces a no-op diff.</done>
</task>

</tasks>

<verification>
- `cargo test --no-default-features --features gleam` passes standalone.
- `cargo test --all-features` still passes.
- `eval/corpus/gleam/scoped_call/` resolves its one expected `Call` edge.
- `docs/supported-languages.md`'s sync test passes with the new row.
- `bindings/node`'s napi artifacts are diff-free after a real rebuild.
- `cargo fmt --all` and `cargo clippy --workspace --all-targets --all-features -- -D warnings` clean.
</verification>

<success_criteria>
- Roadmap Phase 4 criterion 5 (Gleam, having passed Phase 1's ABI gate, emits real
  symbols/Calls/Imports on `.gleam`) is met.
- Roadmap criterion 6 (isolated feature test, bindings parity, napi diff-free, corpus, docs row) is
  met for Gleam.
- LANG-12's Gleam member is closed (Elixir/Erlang/Haskell follow in 04-04..04-06).
</success_criteria>

<output>
After completion, create `.planning/phases/04-risky-novel-design-extractors/04-03-SUMMARY.md`
</output>
</content>
