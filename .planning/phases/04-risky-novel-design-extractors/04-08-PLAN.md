---
phase: 04-risky-novel-design-extractors
plan: 08
type: execute
wave: 8
depends_on: ["04-07"]
files_modified:
  - src/lang.rs
  - Cargo.toml
  - bindings/node/Cargo.toml
  - bindings/python/Cargo.toml
  - src/extract/r.rs
  - src/extract/mod.rs
  - src/extract/dispatch.rs
  - eval/corpus/r/scoped_call/math.R
  - eval/corpus/r/scoped_call/expected.edges
  - docs/supported-languages.md
autonomous: true
requirements: [LANG-03]

must_haves:
  truths:
    - "extract() on a .R/.r file emits assignment-based function symbols (f <- function(...), also = and <<- where AST-unambiguous) via one uniform RHS-kind check"
    - "extract() gives Visibility::Unknown on every R symbol (no in-language public/private signal; NAMESPACE files explicitly out of scope)"
    - "library()/require()/requireNamespace() calls are emitted as Import references (both bare-identifier and string-literal argument forms handled); pkg::fn / pkg:::fn namespace-qualified calls capture the qualifier"
    - "setClass/setGeneric/setMethod calls are recorded as ordinary Call references, NEVER synthesized into class symbols (D-02, explicit requirement)"
    - "eval(parse(text=...))-style NSE is documented as an unresolved ceiling, never attempted"
    - "cargo test --no-default-features --features r passes standalone"
  artifacts:
    - path: "src/extract/r.rs"
      provides: "RExtractor — binary_operator RHS-kind function detection, library/require import classification, namespace-operator qualified calls, S4-as-plain-calls"
      min_lines: 130
    - path: "eval/corpus/r/scoped_call/expected.edges"
      provides: "golden same-file Call edge"
    - path: "docs/supported-languages.md"
      provides: "R row moved 🟠 → 🟢 with honest capability columns"
  key_links:
    - from: "src/extract/dispatch.rs"
      to: "src/extract/r.rs"
      via: "RExtractor match arm under #[cfg(feature = \"r\")]"
      pattern: "RExtractor"
    - from: "bindings/node/Cargo.toml"
      to: "Cargo.toml"
      via: "explicit features list gains \"r\" in the same change"
      pattern: "\"r\""
---

<objective>
Ship the R extractor end-to-end — the LAST plan of Phase 4 and the milestone's final new-language
extractor. D-02's defining technique: `f <- function(...)`, `f = function(...)`, and
`f <<- function(...)` ALL produce the IDENTICAL `binary_operator lhs: rhs:` shape (verified — no
distinguishing field); a single "is the RHS a `function_definition`?" check handles all three
assignment operators uniformly, no operator-text branching needed.

Purpose: Close LANG-03 with two honest ceilings held to the letter: `Visibility` is ALWAYS `Unknown`
(R has no in-language public/private signal), and NSE (`eval(parse(text=...))`) is documented, never
attempted. Also closes D-02's explicit anti-pattern: S4 `setClass`/`setGeneric`/`setMethod` calls stay
ordinary Call references — they must NOT be synthesized into class symbols.
Output: `src/extract/r.rs`, updated `src/lang.rs`/`Cargo.toml`/both bindings `Cargo.toml`s,
`eval/corpus/r/scoped_call/`, updated docs row. This is the final plan — Phase 4 (and the milestone's
LANG-* requirement set) is complete after this plan lands.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-risky-novel-design-extractors/04-CONTEXT.md
@.planning/phases/04-risky-novel-design-extractors/04-RESEARCH.md
@src/extract/objc.rs
@src/extract/fortran.rs
@src/extract/support.rs
@src/extract/lua.rs
@src/lang.rs
@src/extract/mod.rs
@src/extract/dispatch.rs
</context>

<interfaces>
`Descriptor::Method { name: String, disambiguator: String }` — `disambiguator: String::new()` (R has
no arity-overload identity concern comparable to Julia/Elixir/Erlang; table-stakes scope is top-level
assignment-based definitions only, one per name).
`support.rs`'s `collect_call_references` accepts an optional `@qualifier` capture (used here for
`pkg::fn`/`pkg:::fn`).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Wire Language::R + Cargo feature flip + bindings feature lists</name>
  <read_first>src/extract/objc.rs, src/extract/fortran.rs, src/extract/support.rs, src/lang.rs (whole file), Cargo.toml (features block), bindings/node/Cargo.toml, bindings/python/Cargo.toml</read_first>
  <files>src/lang.rs, Cargo.toml, bindings/node/Cargo.toml, bindings/python/Cargo.toml</files>
  <action>
1. `src/lang.rs`: add `R, // .R, .r` to the `Language` enum (append after `Julia` — this is the LAST
   variant in the enum after this plan). Add `Language::R` to `Language::ALL`. Add
   `Language::R => &["r", "R"],` to `extensions()` — NOTE: per D-02/04-RESEARCH.md the docs table
   lists `.r`/`.R` in that order; confirm `from_extension`'s lowercasing (`Language::from_path` already
   lowercases before matching, so listing both cased forms in `extensions()` is for the docs-sync test
   only — `"r"` alone would suffice for matching, but list both to keep the primary-extension doc-sync
   token literal (`` `.r` ``) consistent with the existing 🟠 placeholder row). Add
   `Language::R => "r",` to `as_str()`. Add `Language::R` to `assert_variant_in_all`.

2. `Cargo.toml`: change `r = ["dep:tree-sitter-r"]` to `r = ["dep:tree-sitter-r", "_extractors"]`.
   Append `"r"` to `default = [...]` right after `"julia"` — this is the FINAL entry in `default`
   after this plan.

3. `bindings/node/Cargo.toml` AND `bindings/python/Cargo.toml`: append `"r"` to the `features` list on
   the `code2graph_core` dependency line in BOTH files (both currently end
   `..., "haskell", "julia"]`).

Do NOT create `src/extract/r.rs` yet — Task 2.
  </action>
  <verify>
    <automated>cargo check --no-default-features --features r</automated>
    <automated>cargo test --lib lang::tests</automated>
  </verify>
  <acceptance_criteria>
    <check>grep -q "^    R, " src/lang.rs</check>
    <check>grep -q '"r", "R"' src/lang.rs</check>
    <check>grep -q 'r = \["dep:tree-sitter-r", "_extractors"\]' Cargo.toml</check>
    <check>grep -q '"julia", "r"' Cargo.toml</check>
    <check>grep -q '"julia", "r"' bindings/node/Cargo.toml</check>
    <check>grep -q '"julia", "r"' bindings/python/Cargo.toml</check>
  </acceptance_criteria>
  <done>Language::R variant compiles with .R/.r extension dispatch; r feature gains _extractors and
  sits in default (the final entry); both bindings' feature lists updated (also final entries).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RExtractor — RHS-kind function detection, library/require imports, qualified calls</name>
  <read_first>src/extract/objc.rs, src/extract/lua.rs (script-language module doc style, no in-language namespace precedent — direct R template per research's "template: Lua"), src/extract/support.rs</read_first>
  <files>src/extract/r.rs, src/extract/mod.rs, src/extract/dispatch.rs</files>
  <behavior>
    - Test: `foo <- function(x, y) { x + y }` → a Function symbol "foo" with
      `visibility == Visibility::Unknown`.
    - Test: `bar = function(z) { z * 2 }` → ALSO a Function symbol "bar" (SAME `binary_operator` shape,
      `=` instead of `<-` — the RHS-kind check is operator-text-agnostic).
    - Test: `counter <<- function() { 1 }` → ALSO a Function symbol "counter" (`<<-` — same shape
      again — the uniformity-across-three-operators regression test).
    - Test: `result <- foo(1, 2)` → a Call reference "foo" (qualifier `None`).
    - Test: `other <- utils::head(c(1, 2, 3))` → a Call reference "head" with
      `qualifier == Some("utils")` (`namespace_operator`).
    - Test: `utils:::internal_fn(5)` → ALSO a Call reference "internal_fn" with
      `qualifier == Some("utils")` (`:::` renders IDENTICALLY to `::` — not distinguished, per D-02).
    - Test: `library(utils)` (bare-identifier arg) AND `library("utils")` (string-literal arg) → BOTH
      produce an Import reference "utils" — the double-form regression test (R accepts both syntaxes).
    - Test: `require(dplyr)` / `requireNamespace(dplyr)` → also Import references.
    - Test: `setClass("Person", representation(name = "character"))` → an ORDINARY Call reference named
      "setClass" — assert NO Class/Struct symbol named "Person" is emitted (D-02's explicit
      anti-synthesis requirement).
  </behavior>
  <action>
Create `src/extract/r.rs`. Module doc header: state D-02's scope (assignment-based function symbols
via uniform RHS-kind check across `<-`/`=`/`<<-`, `library`/`require`/`requireNamespace` imports,
namespace-qualified calls, S4 calls recorded as ordinary calls) and BOTH ceilings verbatim:
`Visibility` is ALWAYS `Unknown` (no in-language public/private signal; NAMESPACE-file visibility
explicitly out of scope this milestone); `eval(parse(text=...))`-style NSE is documented, never
attempted.

**Namespace derivation**: `r_namespaces(file)` — strip `.R`/`.r`, strip `src/`, split on `/` (R has no
in-language namespace declaration — file-path-derived identity, same as Lua's approach per research's
"template: Lua").

**Symbol collection** (verified AST, 04-RESEARCH.md "R" Code Examples section — top-level scan only,
document that nested/local function assignments inside another function body are NOT emitted as
symbols, a table-stakes scope limitation):
Walk `root`'s DIRECT children (top-level only). For each `binary_operator lhs: (identifier) rhs: (...)`
node: if the `rhs` field's node kind is `"function_definition"` → emit `SymbolKind::Function` +
`Descriptor::Method { name: lhs_text, disambiguator: String::new() }`, `Visibility::Unknown`. This ONE
check (RHS kind, ignoring the operator's own token text) uniformly covers `<-`, `=`, and `<<-` — do
NOT branch on the operator text, it plays no role in whether the RHS is a definition (verified
empirically per research's Don't-Hand-Roll entry).

**Calls**:
```rust
const CALL_QUERY: &str = r#"
[
  (call function: (identifier) @callee arguments: (arguments))
  (call function: (namespace_operator lhs: (identifier) @qualifier rhs: (identifier) @callee) arguments: (arguments))
]
"#;
```
Run via `collect_call_references`. This ALSO captures `setClass`/`setGeneric`/`setMethod` calls as
ordinary Call references automatically — do NOT add special-case logic to synthesize a class symbol
from them (D-02's explicit anti-pattern; the absence of special-casing IS the correct behavior — note
this explicitly in the module doc so it reads as an intentional design choice, not an oversight).

**Import reclassification** (mirrors `lua.rs`'s `require()`-as-Import precedent exactly): after
collecting the generic call list above, for every Call reference whose `name` is `"library"`,
`"require"`, or `"requireNamespace"` AND whose call site has EXACTLY one argument, re-tag it as
`RefRole::Import` using that argument's resolved text as the import name. The argument may be a bare
`identifier` (`library(utils)`) OR a `string` literal (`library("utils")`, unquoted via `super::unquote`
or manual quote-stripping) — handle BOTH forms; do not assume only one syntax appears. Implement this
as a manual walk over `call` nodes (not a reuse of the generic `CALL_QUERY` results) so the argument
node itself (not just its text via the query capture) is available for the identifier-vs-string-literal
branch.

**Wiring**: `src/extract/mod.rs` — `#[cfg(feature = "r")] pub mod r;` /
`#[cfg(feature = "r")] pub use r::RExtractor;` (alphabetical position — note the single-letter module
name; place near `python`/`ruby` alphabetically by the SORT KEY `r`, i.e. before `ruby`). Update
`src/extract/dispatch.rs` with the matching `use` + `Language::R => RExtractor.extract(source, file),`
arm.

Minimal `scopes[0]` = `Module` file-root scope, `attach_reference_scopes`,
`definition_bindings(&defs)`.
  </action>
  <verify>
    <automated>cargo test --no-default-features --features r extract::r::tests</automated>
    <automated>cargo test --no-default-features --features r</automated>
  </verify>
  <acceptance_criteria>
    <check>grep -q "struct RExtractor" src/extract/r.rs</check>
    <check>grep -q "function_definition" src/extract/r.rs</check>
    <check>grep -q "namespace_operator" src/extract/r.rs</check>
    <check>grep -q "requireNamespace" src/extract/r.rs</check>
    <check>grep -q "RExtractor" src/extract/dispatch.rs</check>
    <check>grep -q "pub mod r;" src/extract/mod.rs</check>
  </acceptance_criteria>
  <done>RExtractor compiles, is wired into dispatch, and its unit tests prove: uniform RHS-kind
  function detection across &lt;-/=/&lt;&lt;-; Visibility::Unknown on every symbol; both
  library()/require() argument forms classified as Import; :: and ::: namespace-qualified calls;
  setClass/setGeneric/setMethod stay ordinary calls, never synthesized into symbols.</done>
</task>

<task type="auto">
  <name>Task 3: Corpus case + docs row + bindings verify (final phase gate)</name>
  <read_first>src/extract/r.rs (from Task 2), eval/corpus/fortran/scoped_call/ (format example), docs/supported-languages.md, src/extract/support.rs</read_first>
  <files>eval/corpus/r/scoped_call/math.R, eval/corpus/r/scoped_call/expected.edges, docs/supported-languages.md</files>
  <action>
**Corpus case** (`eval/corpus/r/scoped_call/`):

`math.R`:
```r
get_base <- function() {
  41
}

compute <- function() {
  get_base() + 1
}
```

`expected.edges`:
```
# Same-file call: `compute` calls `get_base`, both assignment-based functions
# in math.R. Proves R binary_operator RHS-kind function detection resolves
# via Tier-A same-file matching.
math.R:6 Call math.R:1
```
(Verify exact line numbers against the real parse before committing.)

**Docs row** (`docs/supported-languages.md`): replace the R 🟠 row (`.r` `.R`) with:
```
| R               | `.r` `.R`                               |   🟢   |   ✓   |    ✓    |    —    |    —     |            |           | assignment-based function detection (`<-`/`=`/`<<-` share one RHS-kind check; top-level scan only — nested/local function assignments not emitted as symbols); `Visibility` always `Unknown` (no in-language signal; NAMESPACE files out of scope); `library()`/`require()`/`requireNamespace()` → Import (bare-identifier and string-literal arg forms both handled); `pkg::fn`/`pkg:::fn` namespace-qualified calls captured (`::` vs `:::` not distinguished — identical grammar shape); `setClass`/`setGeneric`/`setMethod` recorded as ordinary calls, never synthesized into class symbols; NSE/`eval(parse(text=...))` unresolved |
```

**Bindings verify (BIND-02)**: `cd bindings/node && npx napi build --release --platform`, confirm
`index.js`/`index.d.ts` diff is a no-op.

**Phase-final full-workspace gate** (this is the LAST plan of Phase 4 — run the complete gate, not
just the isolated-feature checks every prior task ran):
```bash
cargo fmt --all
cargo clippy --workspace --all-targets --all-features -- -D warnings
cargo test --all-features
```
  </action>
  <verify>
    <automated>cargo test --no-default-features --features r</automated>
    <automated>cargo test --all-features corpus</automated>
    <automated>cd bindings/node && npx napi build --release --platform && git diff --exit-code index.js index.d.ts</automated>
    <automated>cargo test --lib lang::tests::supported_languages_doc_lists_each_primary_extension</automated>
    <automated>cargo fmt --all -- --check</automated>
    <automated>cargo clippy --workspace --all-targets --all-features -- -D warnings</automated>
    <automated>cargo test --all-features</automated>
  </verify>
  <acceptance_criteria>
    <check>test -f eval/corpus/r/scoped_call/expected.edges</check>
    <check>grep -q "^| R " docs/supported-languages.md</check>
  </acceptance_criteria>
  <done>Corpus case resolves its expected Call edge; docs row is 🟢 with the Unknown-visibility and
  NSE ceilings honestly documented in Notes; napi build produces a no-op diff; the full workspace gate
  (fmt/clippy/all-features test) passes clean, completing Phase 4 and this milestone's LANG-*
  requirement set.</done>
</task>

</tasks>

<verification>
- `cargo test --no-default-features --features r` passes standalone.
- `cargo test --all-features` passes for the full workspace (all 32 languages).
- `eval/corpus/r/scoped_call/` resolves its one expected `Call` edge.
- `docs/supported-languages.md`'s sync test passes with the new row.
- `bindings/node`'s napi artifacts are diff-free after a real rebuild.
- `cargo fmt --all` and `cargo clippy --workspace --all-targets --all-features -- -D warnings` clean.
- All eight Phase 4 languages (OCaml, F#, Gleam, Elixir, Erlang, Haskell, Julia, R) have shipped
  extractors, corpus cases, 🟢 docs rows, and bindings-parity entries by the time this plan completes.
</verification>

<success_criteria>
- Roadmap Phase 4 criterion 2 (R assignment-based function symbols, `Visibility::Unknown`, NSE
  ceiling documented) is met.
- Roadmap criterion 6 (isolated feature test, bindings parity, napi diff-free, corpus, docs row) is
  met for R.
- LANG-03 is closed. All five Phase 4 requirement IDs (LANG-02, LANG-03, LANG-04, LANG-11, LANG-12)
  are now closed; the milestone's full v1 LANG-* requirement set is complete.
</success_criteria>

<output>
After completion, create `.planning/phases/04-risky-novel-design-extractors/04-08-SUMMARY.md`
</output>
</content>
